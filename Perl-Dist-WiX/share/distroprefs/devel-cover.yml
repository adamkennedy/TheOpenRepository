---
comment: |

  Devel::Cover needs a patch to install in Strawberry Perl (RT# 50774)

match:
  distribution: |
    ^(?x:
    .^   # never matches, only purpose is to let things align nicely
    |PJCJ/Devel-Cover-0.65.tar.gz          # Needs a patch to install.
    )|
patches:
    - "Devel-Cover-0.65.patch"
