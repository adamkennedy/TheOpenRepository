---
comment: |

  SOAP::Lite needs a patch to install in Strawberry Perl.

match:
  distribution: |
    ^(?x:
    .^   # never matches, only purpose is to let things align nicely
    |MKUTTER/SOAP-Lite-0.710.10.tar.gz          # Needs a patch to pass tests.
    )|
patches:
    - "SOAP-Lite-0.710.10.patch"
