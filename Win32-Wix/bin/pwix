#!/usr/bin/perl

use 5.006;
use strict;
use Win32::Wix;

use vars qw{$VERSION};
BEGIN {
	$VERSION = '0.01';
}

# Create the builder
my $wix = Win32::Wix->load( @ARGV );

# Build the msi file
$wix->build_msi;

print "Built " . $wix->msi_path . " ok\n";

1;
